global:
  scrape_interval: 5s # Le va a preguntar a la API cómo está cada 5 segundos

scrape_configs:
  - job_name: 'amazon_lakehouse_api'
    static_configs:
      - targets: ['host.docker.internal:5214'] # Tu API en C# (Puerto HTTP)